enum ApiErrors {
  request,
  noInternet,
}

enum RequestType {
  get,
  post,
  put,
  delete,
}

enum DeviceType {
  mobile,
  tablet,
  desktop,
}

enum SlidingPanelMenu{
  remove,
  completed,
  notCompleted
}

